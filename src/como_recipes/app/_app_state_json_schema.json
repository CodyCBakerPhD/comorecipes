{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "como_app_state.json",
  "title": "AppState",
  "description": "The underlying data structure for the app state which is saved for a given session.",
  "type": "object",
  "version": "0.0.2",
  "properties": {
    "como_version": {
      "title": "CoMo Version",
      "description": "The version of the CoMo app/package that created this file.",
      "type": "string"
    },
    "home_folder_path": {
      "format": "directory-path",
      "title": "Home Folder Path",
      "description": "The path to the home folder of the CoMo app.",
      "type": "string"
    },
    "base_sessions_folder": {
      "format": "directory-path",
      "title": "Base Folder Sessions",
      "description": "The base path to all sessions meant to be saved or loaded.",
      "type": "string"
    },
    "session_folder_path": {
      "format": "directory-path",
      "title": "Session Folder Path",
      "description": "The path to the specific folder used by the session being saved.",
      "type": "string"
    },
    "app_state_file_path": {
      "format": "file-path",
      "title": "App State File Path",
      "description": "The path to the app state file for this session.",
      "type": "string"
    },
    "meal_selection": {
      "title": "Meal Selection",
      "description": "The meals and custom ingredients that have been selected.",
      "type": "object"
    }
  },
  "required": [
    "version",
    "como_version",
    "home_folder_path",
    "base_sessions_folder",
    "session_folder_path",
    "app_state_file_path",
    "meal_selection"
  ]
}
