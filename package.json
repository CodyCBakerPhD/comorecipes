{
  "name": "como-recipes-site",
  "version": "1.0.0",
  "private": true,
  "description": "CoMo Recipes website — static site generated with TypeScript (no Jekyll)",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node site/build.ts",
    "serve": "npx --yes serve dist"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.3",
    "yaml": "^2.8.0"
  }
}
